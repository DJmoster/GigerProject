version: "3.6"
services:
  python:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 80
    ports:
      - 80:8000
    command: "python manage.py runserver 0.0.0.0:8000"